<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace代表这个*mapper.xml文件指向哪个接口-->
<mapper namespace="dao.DutytypeDao">
    <select id="getAllDutytype" resultType="dao.Dutytype">
        select * from dutytype
    </select>

    <select id="getDutyID" resultType="dao.Dutytype">
        select * from dutytype where dutyname = #{dutyname}
    </select>

</mapper>